This directory contains the following 2 files:
     - ldap_with_groups.cfg
     - preferences.cfg

The ldap_with_groups.cfg must be placed as is into a $SAWMILL_HOME/LogAnalysisInfo/login_plugins directory.

The preferences.cfg should be modified accordingly and placed into the $SAWMILL_HOME/LogAnalysisInfo directory.
