<?xml version="1.0" encoding="UTF-8"?>

<!--
  Copyright 2010-2016, by the California Institute of Technology.
  ALL RIGHTS RESERVED. United States Government Sponsorship acknowledged.
  Any commercial use must be negotiated with the Office of Technology Transfer
  at the California Institute of Technology.

  This software is subject to U. S. export control laws and regulations
  (22 C.F.R. 120-130 and 15 C.F.R. 730-774). To the extent that the software
  is subject to U.S. export control laws and regulations, the recipient has
  the responsibility to obtain export licenses or other export authority as
  may be required before exporting such information to foreign countries or
  providing access to foreign nationals.

  $Id$
-->

<document>
  <properties>
    <title>Installation</title>
    <author email="Hyun.Lee@jpl.nasa.gov">Hyun Lee</author>
    <author email="Sean.Hardman@jpl.nasa.gov">Sean Hardman</author>
  </properties>

  <body>
    <section name="Installation">
      <p>This document describes how to install the <a href="http://www.openldap.org/" target="_blank">OpenLDAP</a> software package. This package serve as the Security Service for the PDS 2010 system. The following topics can be found in this section:
      </p>

      <ul>
        <li><a href="#System_Requirements">System Requirements</a></li>
        <li><a href="#Software_Installation">Software Installation</a></li>
        <li><a href="#Populate_the_Directory">Populate the Directory</a></li>
      </ul>
    </section>

    <section name="System Requirements">
      <p>The software that makes up this project consists of the OpenLDAP open source package that is available for download and installation. This release requires version <i>2.4.44</i> of OpenLDAP. This package requires Java to be installed in the target environment.
      </p>

      <subsection name="Java Runtime Environment">
        <p>The Security Service was developed using Java and will run on any platform with a supported Java Runtime Environment (JRE). The software was specifically compiled for and tested in Java version 1.8. The following commands test the local Java installation in a UNIX-based environment:
        </p>

        <source>
% which java
/usr/bin/java

% java -version
java version "1.8.0_101"
Java(TM) SE Runtime Environment (build 1.8.0_101-b13)
Java HotSpot(TM) 64-Bit Server VM (build 25.101-b13, mixed mode)
        </source>

        <p>The first command above checks whether the <i>java</i> executable is in the environment's path and the second command reports the version. If Java is not installed or the version is not at least 1.8, Java will need to be downloaded and installed in the current environment. Consult the local system administrator for installation of this software. For the do-it-yourself crowd, the Java software can be downloaded from the <a href="http://www.oracle.com/technetwork/java/javase/downloads/" target="_blank">Oracle Java Download</a> page. The suggested software package is the Java Standard Edition (SE) 8, either the JDK or the JRE package. The JDK package is not necessary to run the software but could be useful if development and compilation of Java software will also occur in the current environment.
        </p>
      </subsection>
    </section>

    <section name="Software Installation">
        <p>We are choosing to install a directory server (<a href="http://www.openldap.org" target="_blank">OpenLDAP</a>) so that it is accessible from other applications that require a standard LDAP interface.</p>

        <ul>
          <li>Download the package from <a href="http://www.openldap.org/software/download/" target="_blank">http://www.openldap.org/software/download/</a>.</li>
          <li>Install the package so that it operates on the standard LDAP port 389. The distribution package provides installation documentation. Additional documentation is available at <a href="http://www.openldap.org/doc/admin24/" target="_blank">http://www.openldap.org/doc/admin24/</a>.</li>
          <li>Startup the server.</li>
        </ul>

        <subsection name="Unpack the distribution">
          <p>Change directory to a directory for the source to live under and unpack the distribution using the following commands.</p>
          <source>% gunzip -c openldap-VERSION.tgz | tar xvfB -</source>
          <p>Then relocate yourself into the distribution directory:</p>
          <source>cd openldap-VERSION</source>
        </subsection>
        
        <subsection name="Run configure">
          <p>You will need to run the provided configure script to configure the distribution for building on your system. The configure script accepts many command line options that enable or disable optional software features. Usually the defaults are okay, but you may want to change them. To get a complete list of options that configure accepts, use the --help option.</p>
       	  <source>./configure --help</source>
       	  
       	  <p>However, we'll assume you are okay to just let configure determine what's best.</p>
       	  <source>./configure</source>
       	  <p>By default, it will install the package's files in /usr/local/bin.  You can specify an installation prefix other than `/usr/local' by giving `configure' the option `--prefix=PATH'. For example, </p>
       	  <source>./configure --prefix=YOUR_PATH</source>
        </subsection>
        
        <subsection name="Build the software">
          <p>The next step is to build the software. This step has two parts, first we construct dependencies and then we compile the software. Both <i>makes</i> should complete without error.</p>
          <source>
make depend
make
          </source>
        </subsection>
        
        <subsection name="Test the build">
          <p>To ensure a correct build, you should run the test suite (it only takes a few minutes):</p>
	      <source>
make test
          </source>
		  <p>Tests which apply to your configuration will run and they should pass.</p>
        </subsection>
        
        <subsection name="Install the software">
          <p>You are now ready to install the software; this usually requires super-user privileges:</p>
          <source>
su root -c 'make install'
          </source>
          <p>Everything should now be installed under /usr/local (or whatever installation prefix was used by configure).</p>
        </subsection>
        
        <subsection name="Edit the configuration file">
          <p>1. Use your favorite editor to edit the provided <i>slapd.ldif</i> example (usually installed as /usr/local/etc/openldap/slapd.ldif) to contain a MDB database definition of the form:</p>
          <source>
dn: cn=config
objectClass: olcGlobal
cn: config
olcReferral: ldap://pdsdev.jpl.nasa.gov:1636
olcSizeLimit: 10000

olcSuffix: dc=pdsdev,dc=jpl,dc=nasa,dc=gov
olcRootDN: cn=config,dc=pdsdev,dc=jpl,dc=nasa,dc=gov
# Cleartext passwords, especially for the rootdn, should
# be avoided.  See slappasswd(8) and slapd-config(5) for details.
# Use of strong authentication encouraged.
olcRootPW: {password}

# Define global ACLs to disable default read access.
#
olcArgsFile: /home/hyunlee/openldap/var/run/slapd.args
olcPidFile: /home/hyunlee/openldap/var/run/slapd.pid
olcLogFile: /home/hyunlee/openldap/var/run/slapd.log
olcLogLevel: stats2 stats ACL config filter conns

...
...
...

#######################################################################
# LMDB database definitions
#######################################################################
dn: olcDatabase=mdb,cn=config
objectClass: olcMdbConfig
olcDatabase: mdb
objectClass: olcDatabaseConfig

olcSuffix: "dc=pdsdev,dc=jpl,dc=nasa,dc=gov"
olcRootDN: "cn=Manager,dc=pdsdev,dc=jpl,dc=nasa,dc=gov"
# Cleartext passwords, especially for the rootdn, should
# be avoided.  See slappasswd(8) and slapd-config(5) for details.
# Use of strong authentication encouraged.
olcRootPW: {password}

# The database directory MUST exist prior to running slapd AND 
# should only be accessible by the slapd and slap tools.
# Mode 700 recommended.
olcDbDirectory: /home/hyunlee/openldap/var/openldap-data
# Indices to maintain
olcDbIndex: objectClass eq 
olcDbIndex: cn,sn,uid pres,eq,approx,sub
          </source>
          
          <p>2. Copy <a href="../docs/PDSPerson.schema" target="_blank"><i>PDSPerson.schema</i></a> file to the $OPENLDAP_HOME/etc/openldap/schema. This configuration file specifies the schema for the directory server.</p>
          
          <p>3. Edit <i>slapd.conf</i> file to contain following information.</p>
          <source>
include         /home/hyunlee/openldap/etc/openldap/schema/core.schema
include         /home/hyunlee/openldap/etc/openldap/schema/cosine.schema
include         /home/hyunlee/openldap/etc/openldap/schema/inetorgperson.schema
include         /home/hyunlee/openldap/etc/openldap/schema/PDSPerson.schema

pidfile         /home/hyunlee/openldap/var/run/slapd.pid
argsfile        /home/hyunlee/openldap/var/run/slapd.args
logfile         /home/hyunlee/openldap/var/run/slapd.log

...
...
...

#######################################################################
# MDB database definitions
#######################################################################
database        mdb
maxsize         1073741824
suffix          "dc=pdsdev,dc=jpl,dc=nasa,dc=gov"
rootdn          "cn=Manager,dc=pdsdev,dc=jpl,dc=nasa,dc=gov"
rootpw          {password}

# The database directory MUST exist prior to running slapd AND 
# should only be accessible by the slapd and slap tools.
# Mode 700 recommended.
directory       /home/hyunlee/openldap/var/openldap-data
# Indices to maintain
index   objectClass     eq

# allow users to change their password
access to attrs=userPassword
        by self write
        by anonymous auth
        by users none
access to * by * read
          </source>       
        </subsection>
        
        
        <subsection name="Import the configuration database">
          <p>You are now ready to import your configration database for use by <i>slapd</i>, by running the commands. </p>
          <source>
% setenv LDAPCONF $OPENLDAP_HOME/etc/openldap/slapd.d
% setenv LDAPRC $OPENLDAP_HOME/etc/openldap/slapd.d     
% mkdir $OPENLDAP_HOME/etc/openldap/slapd.d

% $OPENLDAP_HOME/sbin/slaptest -F $OPENLDAP_HOME/etc/openldap/slapd.d -f $OPENLDAP_HOME/etc/openldap/slapd.conf -n 0     
          </source>
        </subsection>
        
        <subsection name="Start the SLAPD">
          <p>You are now ready to start the Standalone LDAP Daemon, <i>slapd</i>, by running the command.</p>
		  <source>
% su root -c $OPENLDAP_HOME/libexec/slapd -F $OPENLDAP_HOME/etc/openldap/slapd.d -h ldap://pdsdev.jpl.nasa.gov:389 -d -1 &amp;
          </source>
          
          <p>To check to see if the server is running and configured correctly, you can run a search against it with <i>ldapsearch</i>. By default, ldapsearch is installed as /usr/local/bin/ldapsearch:</p>
          <source>
% $OPENLDAP_HOME/bin/ldapsearch -x -b '' -s base '(objectclass=*)' namingContexts
          </source>
        </subsection>
    </section>




    <section name="Populate the Directory">
      <subsection name="Generate LDIF file(s)">
      <p>Once OpenLDAP is installed and function, we can populate the information. The following command generates two LDIF file(s) for your LDAP directory.</p>
      <p><i><b>pds_personnel.ldif</b></i> is for adding the PDS personnel entries into the direcotry server. <i><b>pds_personnel_delete.ldif</b></i> is for deleting the personnel entries from the directory server.</p>

<source>
% java gov.nasa.pds.security.LDIFGenerator -t {directory of personnel files reside} \
   -s {host name} -n {port number} -p {password}
</source>
      </subsection>

      <subsection name="Directory Server Configuration">
        <p>With the software configuration complete, it is time to add groups and users to the directory server. The following configuration files, in the LDAP Data Interchange Format (LDIF), are available for configuring the directory server:
        </p>


        <ul>
          <li><a href="../docs/pds_groups.ldif" target="_blank">pds_groups.ldif</a><br/>
            This configuration file specifies the PDS group entries for the directory server.
            <p/>
          </li>
          <li><a href="../docs/pds_personnel.ldif" target="_blank">pds_personnel.ldif</a><br/>
            This configuration file specifies PDS person entries for the directory server. The actual PDS person entries have been removed from this example configuration file since it contains clear text passwords. This file contains an example person entry for the <i>pdsmc</i> account.
          </li>
        </ul>

        <p>Execute the commands with the configuration files as follows:
        </p>

        <source>
% $OPENLDAP_HOME/bin/ldapadd -x -D "cn=Manager,dc=pdsdev,dc=jpl,dc=nasa,dc=gov" -w {password} \
    -f /home/hyunlee/openldap/pds_groups.ldif
    
% $OPENLDAP_HOME/bin/ldapadd -x -D "cn=Manager,dc=pdsdev,dc=jpl,dc=nasa,dc=gov" -w {password} \
    -f /home/hyunlee/openldap/pds_personnel.ldif
        </source>
        
      </subsection>

    </section>
  </body>
</document>
